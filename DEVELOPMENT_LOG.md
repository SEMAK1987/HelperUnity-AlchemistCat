# DEVELOPMENT LOG

## [2026-09-08] - Версия 18.12.53
- **Алхимическая Рыбалка: Кнопка Закрыть/Назад и точная геометрия полос**:
  - `HandleCloseClicked()`: при нажатии на крестик (`Close_Button`) во время ловли на пруду или в окне итогов игра возвращает игрока в меню выбора сложности (`ShowDifficultySelection()`), позволяя сменить режим, не закрывая окно полностью.
  - Симметричное растяжение лучей: исправлен расчет `pivot` и `localScale` для левого и правого лучей (`pivot.x = 1` для левого, `pivot.x = 0` для правого, `localScale = Vector3.one`), лучи теперь расходятся непрерывно и симметрично от центра в обе стороны.
  - Ограничение размеров: высота полосы установлена в 42 px (не вылезает за верх/низ золотой рамки), а максимальная длина ограничена 68% внутренней части рамки.

## [2026-05-14]
- Версия 18.5.8: Zenith Multi-Tool Synergy & Settings Fix.

